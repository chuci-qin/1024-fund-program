{
  "name": "1024-fund-program-scripts",
  "version": "1.0.0",
  "description": "Scripts for managing 1024 Fund Program configurations",
  "scripts": {
    "deploy": "./deploy_pm_fee.sh",
    "init": "node init_pm_fee_config.js",
    "query": "node query_pm_fee_config.js",
    "update": "node update_pm_fee_config.js",
    "pause": "node set_pm_fee_paused.js --paused true",
    "resume": "node set_pm_fee_paused.js --paused false"
  },
  "dependencies": {
    "@solana/web3.js": "^1.87.0",
    "@solana/spl-token": "^0.3.8"
  }
}

